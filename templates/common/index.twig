 <head>
        <meta charset="utf-8" />
        <title>{% block title %} {% endblock %}</title>
</head>
<body>
	{% if not(connected) %}
		{% include 'common/sign-in-form.twig' %}
	{% else %}
        {% block header %} {% endblock %}
        {% block body %} {% endblock %}
        {% block footer %} {% endblock %}
    {% endif %}
</body>